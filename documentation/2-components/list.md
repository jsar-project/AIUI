# List 列表

`list` 是 `list-item` 的容器，使用 CSS 控制排列方向、间距和标记样式。

## 展示条目列表

```xml
<list style="display: flex; flex-direction: column; gap: 8px;">
  <list-item>准备数据</list-item>
  <list-item>生成界面</list-item>
</list>
```
