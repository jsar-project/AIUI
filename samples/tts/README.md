# Minimax TTS Sample

This sample is a focused Minimax WebSocket TTS demo.

- Shows the request and PCM playback flow
- Lets you inject your own authorization provider
- Keeps the sample self-contained with its own `lib/tts.js` and `lib/request.js`

## Sanitized By Default

This sample does not bundle live credentials or a built-in authorization flow.
